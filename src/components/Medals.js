import React from 'react';
import { Typography, Divider, Layout, Tag, Select } from 'antd';

const { Title } = Typography;

const Medals = () => {
    return (
        <div>
            <Title level={3}>Médailles</Title>
            
        </div>
    );
};

export default Medals;